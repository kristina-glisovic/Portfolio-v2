{
  "name": "kristina-glisovic-portfolio",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check",
    "check:production": "node scripts/check-production.mjs",
    "test:contact": "node --test scripts/contact-endpoint.test.mjs",
    "test:worker": "node --test scripts/locale-routing.test.mjs scripts/contact-endpoint.test.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
